# Assay reference archive note

The full 1536x1024 generated source reference and the assembled comparison
sheet were retained in the local QA archive rather than the release branch
because they exceed the GitHub connector's single-blob transport limit.

Release-visible evidence remains available in:

- `assets/ag-assay-monolith-1280.webp` and
  `assets/ag-assay-monolith-mobile.webp` for the owned production fallback;
- `docs/qa/3d-captures/` for the individual match, side, grazing, wide,
  tablet, fallback, and lifecycle captures;
- `.img2threejs/spec/`, `.img2threejs/intake/`, and the reconstruction report
  for the object specification and locally extracted PBR evidence.

The archived source reference SHA-256 is
`5b86deb1febfa7f17088969d7b4f25f78c59408c4ea4e025efd5d737771c4b6f`.
The archived comparison sheet SHA-256 is
`7cf009a1feff50b3dc01f3629f866235b81a33e6e23afdb0ce1361e2fa26dce3`.
